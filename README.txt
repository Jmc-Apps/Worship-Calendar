The Worship Calendar v2.04
Plan • Schedule • Serve

Built from v2.03.

Fixed:
- Admin password changes now update both the protected admin account and legacy admin password setting.
- Login now checks saved member.role and logs admin-role users into admin mode.
- Default admin account is normalized and protected on startup.
- Default admin cannot lose admin role.
- JavaScript syntax checked before packaging.
