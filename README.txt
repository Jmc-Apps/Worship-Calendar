The Worship Calendar v2.53

Emergency clean rebuild:
- Rebuilt from clean v2.45 baseline.
- Fixed the script-boundary bug that caused JavaScript text to display on app load.
- Replaced the main script before changing HTML, so script positions cannot shift.
- Added Used, ↑, ↓ together in the left Use Role column.
- Up/down buttons use v2.53 handlers only.
- Unused roles are automatically kept at the bottom.
- Template role order is used by roster generation and roster display.
- Main JavaScript syntax check passed.
- ZIP cleanup: only this README.txt is included.
