The Worship Calendar v2.76

Fix:
- Properly added a visible Connect Gmail button to the Email Team tab.
- The Email Team Connect Gmail button calls the same connectGmail() function used in Settings.
- Added a Test Gmail Connection button beside it.
- Added a DOM fallback to ensure the Gmail Connection panel appears on the Email Team tab even if the page is rendered by an older function.
- Preserved v2.75 robust login repair.
- Preserved API setup import, welcome screen fix, roster ministry filtering, data recovery, availability fix, merge-before-save, and linked member rules.
- Removed icon.svg from package/cache references.
- Main JavaScript syntax check passed.
- ZIP cleanup: only this README.txt is included.
