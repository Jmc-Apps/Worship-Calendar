The Worship Calendar v2.56

Change:
- Added Full Database Backup & Restore to the Admin Settings tab.
- Export creates a JSON backup of the entire app database.
- Restore imports a full backup and replaces the local database on the device.
- Backup includes members, ministries, roles, templates, services, availability, rosters, settings and API configuration.
- Existing API Backup remains available separately.
- Preserves v2.55 member max-services control, v2.54 same-date rostering rules, and v2.53 template role ordering.
- Main JavaScript syntax check passed.
- ZIP cleanup: only this README.txt is included.
