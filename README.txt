The Worship Calendar v2.46

Changes:
- Added per-template role sort order support.
- Each template can store a roleOrder array.
- Roster generation uses the template role order when building assignments.
- Roster display uses the template role order when showing assignments.
- PDF/export assignment ordering is patched to use the same template role order where roster assignment iteration is used.
- Template role order helpers preserve roles added to or removed from templates.
- Preserves v2.45 month-aware batch planning, hard-coded admin exclusion, No Roster markers, and draft roster delete button.
- ZIP cleanup: only this README.txt is included.
