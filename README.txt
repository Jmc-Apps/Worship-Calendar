The Worship Calendar v2.47

Changes:
- Role order controls are now inline with the role ordering/list controls.
- Unused roles are automatically moved to the bottom of each template's roleOrder.
- Unused roles cannot be moved above active roles unless selected for the template.
- Roster generation, roster display, and export ordering continue to use the template role order.
- Preserves v2.45 month-aware batch planner, hard-coded admin exclusion, No Roster markers, and draft roster delete button.
- ZIP cleanup: only this README.txt is included.
