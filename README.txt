The Worship Calendar v2.57

Changes:
- Templates tab: admins can choose which ministries are included in each template.
- Only selected ministries and their roles are shown/used in that template.
- If a ministry is removed from a template, its roles are removed from that template's active role list.
- Roster generation now respects the ministries selected for the template.
- Services tab: selecting a template in the new service form automatically changes the service name to that template name.
- Preserves v2.56 full database backup/restore, v2.55 member max-services control, v2.54 same-date rostering rules, and v2.53 template role ordering.
- Main JavaScript syntax check passed.
- ZIP cleanup: only this README.txt is included.
