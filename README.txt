The Worship Calendar v2.89

Fix:
- Fixed the PDF row colour pop-up palette showing grey blocks.
- Colour palette blocks now use explicit visible 50% opacity rgba colours.
- Preview block also uses the same visible 50% opacity colour.
- Palette blocks are slightly larger for easier selection.
- Selected colour still saves to the existing PDF row colour fields.

Preserved:
- v2.88 pop-up PDF row colour picker.
- v2.86 compact Delete Services checklist.
- v2.82 deletion-aware merge protection.
- v2.81 API setup persistence and GitHub 409 retry.
- Previous login, Gmail, roster filtering, linked-member and merge-save features.
- Removed icon.svg from package/cache references.
- Main JavaScript syntax check passed.
- ZIP cleanup: only this README.txt is included.
