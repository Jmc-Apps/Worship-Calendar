The Worship Calendar v2.59

Changes:
- PDF roster export now uses the template role sort order for role rows.
- Roles tab now lets admins choose a PDF row colour for each role.
- The selected colour is applied to the role row in exported/printed PDF rosters.
- Role colours are stored in the app database under roleColors.
- Preserves v2.58 service-name-from-template behaviour and all previous working features.
- Main JavaScript syntax check passed.
- ZIP cleanup: only this README.txt is included.
